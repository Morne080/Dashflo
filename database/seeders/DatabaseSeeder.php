<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Fresh installs should stay empty: register via the app and connect real integrations / imports.
     *
     * Optional demo dataset (~1k leads + suppliers/buyers): `php artisan db:seed --class=DashfloSeeder`
     * Optional default Overview dashboard widgets (first user): `php artisan db:seed --class=DashboardSeeder`
     */
    public function run(): void
    {
        //
    }
}
